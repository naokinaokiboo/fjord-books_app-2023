<p style="color: green"><%= notice %></p>

<div class="show-item">
  <%= render @book %>
</div>

<nav class="page-nav">
  <%= link_to t('book.link.to_edit'), edit_book_path(@book) %> |
  <%= link_to t('book.link.to_books'), books_path %>

  <%= button_to t('book.button.destroy'), @book, method: :delete %>
</nav>
