<h1><%= t('views.common.title_edit', name: Comment.model_name.human.downcase) %></h1>

<%= render partial: "form", locals: {commentable: @commentable, comment: @comment } %>

<br>

<nav class="page-nav">
  <%= link_to t('views.common.back_to_target', name: Comment.model_name.human.downcase),  @commentable %>
</nav>
